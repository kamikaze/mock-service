[package]
name = "mock-service"
version = "0.0.12"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["rt", "rt-multi-thread", "macros", "fs"] }
axum = "0.8.9"

[profile.release]
codegen-units = 1
debug = false
lto = "fat"
opt-level = "z"
strip = true
